# マイアプリおよび管理ツール アーキテクチャ設計

## 1. 要約

### 1.1 プロジェクト背景

本プロジェクトは、日間アクティブユーザー数 10 万人規模に対応する保険系デジタルシステムの開発を目的とする。React Native+TypeScript によるモバイルアプリケーション（App）、React+TypeScript による管理ツール、Spring Boot によるバックエンドサービスで構成され、Microsoft Azure クラウドサービスを基盤としてデプロイと運用を実現する。

### 1.2 プロジェクト目的

1.

## 2. ハイレベルアーキテクチャ

本システムは「フロントエンド-ゲートウェイ-アプリケーションサービス-データストレージ-インフラストラクチャ」の 5 層アーキテクチャを採用し、Azure クラウドサービスを支えとして、クライアントの統一アクセス、ルーティング転送、ビジネス処理、データストレージ、運用監視の全リンクカバレッジを実現する。核心は「軽量ビジネス、高可用性、拡張容易性」に焦点を当て、全体アーキテクチャは以下の通り：

### 2.1 クライアントエントリー

- マイアプリ（App）：エンドユーザー向け、核心は WebView コンポーネントによるマイページ エンドインターフェースコンテンツの読み込みで、SSO シングルサインオン、生体認証、公的個人認証、家族連携、プッシュ通知といった少数のネイティブインタラクションモジュールを提供する；
- 管理ツール（Web エンド）：運用担当者向け、コンテンツの作成・管理、プッシュ通知の管理・送信、プッシュデータの閲覧に使用する；
- マイページ エンド：核心的な契約データのストレージと表示キャリア、SSO シングルサインオンを介して App エンドと接続し、ユーザーが再ログインせずに契約を閲覧できるようにする。

### 2.2 ルーティングとゲートウェイ

- 核心コンポーネント：Azure Front Door Premium + WAF
- 核心機能：
  1. グローバル統一エントリー、ルーティング転送を担当：App エンド、管理ツールエンドのリクエストをそれぞれ対応する App Service インスタンスに転送する；
  2. WAF 保護：OWASP Top 10 保護ルールを有効にし、SQL インジェクション、XSS、CSRF などの悪意のあるリクエストを遮断し、高頻度の悪意的アクセスを制限する；
  3. トラフィック制御とヘルスチェック：単一ユーザーの QPS 制限、定期的にプライマリ・セカンダリ App Service のヘルス状態をチェックし、障害発生時に自動的にトラフィックを切り替える；
  4. CDN 加速：Azure CDN と接続し、静的リソース、動画リソースの高速読み込みを実現し、クライアントのアクセスレイテンシを低減する。

### 2.3 静的および動画リソース

- ストレージコンポーネント：Azure Blob Storage（汎用 Blob + 動画専用 Blob）
- 核心設計：
  1.  汎用 Blob Storage：App エンド、管理ツールエンドの静的リソース（画像、スタイルファイル、設定ファイル）をストレージし、ホットストレージレベルを採用し、プライベートアクセス権限を有効にし、バックエンドサービスと Front Door からのアクセスのみを許可する；
  2.  動画 Blob Storage：システムに必要な動画リソースをストレージし、コールドストレージレベル（アクセス頻度が低い）を採用し、匿名読み取り権限を有効にし、CDN を介して加速読み込みする；
  3.  リソース暗号化：すべてのリソースに Azure 自動暗号化（転送暗号化+ストレージ暗号化）を有効にし、リソースセキュリティを保障する。

### 2.4 全体リンクのフロー

クライアントリクエスト → Azure Front Door Premium（ルーティング転送+WAF 保護+CDN 加速） → プライマリ/セカンダリ App Service（ビジネス処理） → プライマリ/セカンダリ Azure SQL Database（データストレージ）/Blob Storage（リソースストレージ） → レスポンスをクライアントに返却。

## 3. 技術スタック

### 3.1 フロントエンド技術スタック

#### 3.1.1 React Native App（モバイル端末）

- 核心フレームワーク：React Native 0.73+、TypeScript
- 核心コンポーネント/ツール：
  - ページレンダリング：React Native ネイティブコンポーネント + WebView コンポーネント
  - 状態管理：Redux Toolkit（極簡素な使用、軽量な状態のみ管理）
  - ルーティング管理：React Navigation 6.x
  - ネットワークリクエスト：Axios（TS でカプセル化、リクエストインターセプター、レスポンスインターセプターを統一的に処理）
  - ネイティブインタラクション：React Native Modules/TurboMod（e-ninsho SDK、Push 通知と接続）
  - ローカルストレージ：AsyncStorage（軽量ストレージ）、MMKV（高性能キャッシュ）
  - UI コンポーネントライブラリ：React Native Paper/NativeBase（軽量、テーマカスタマイズをサポート）
  - その他ツール：ESLint、Prettier（コード規範）、EAS Build（パッケージングとリリース）

#### 3.1.2 React 管理ツール（Web エンド）

- 核心フレームワーク：React 18+、TypeScript、Vite（ビルドツール）
- 核心コンポーネント/ツール：
  - UI コンポーネントライブラリ：Ant Design Pro（管理エンドシナリオに適合、即時使用可能）
  - 状態管理：Zustand（軽量で定型文なし、状態管理を簡素化）
  - ルーティング管理：React Router 6.x（ルーティングガード、ネストされたルーティング）
  - ネットワークリクエスト：Axios（App エンドと統一的にカプセル化、インターフェースタイプ定義を再利用）
  - データ可視化：ECharts（Push プッシュデータ統計、コンテンツアクセス統計）
  - その他ツール：ESLint、Prettier、Azure Static Web Apps（デプロイ）

### 3.2 バックエンド技術スタック

- 核心フレームワーク：Spring Boot 3.x、Java 17
- 核心コンポーネント/ツール：
  - インターフェース開発：Spring MVC（RESTful API 設計）
  - 永続層：MyBatis-Plus（CRUD 操作を簡素化）、Spring Data JDBC
  - 認証認可：Spring Security、JWT（SSO トークン生成と検証）
  - インターフェースドキュメント：Swagger3（SpringDoc、API ドキュメントを自動生成）
  - キャッシュ：Spring Cache + Redis（Azure Redis Cache、SSO トークン、高頻度データをストレージ）
  - 例外処理：グローバル例外ハンドラー（@RestControllerAdvice）
  - その他ツール：Maven（依存関係管理）、Lombok（コードを簡素化）

### 3.3 Azure クラウドサービス技術スタック

- ゲートウェイとセキュリティ：Azure Front Door Premium + WAF
- アプリケーションサービス：App Service（プライマリインスタンス、セカンダリインスタンス）
- データストレージ：Azure SQL Database（プライマリデータベース、セカンダリデータベース）、Azure Blob Storage（汎用+動画）、Azure Cache for Redis
- メッセージプッシュ：Azure Notification Hubs（App エンド Push 通知配布）
- 監視とログ：Application Insights（全リンク監視）、Log Analytics（ログ収集と分析）、Microsoft Sentinel（セキュリティ分析とアラート）
- セキュリティ保護：Defender for Cloud（クラウドリソースセキュリティ保護）、Azure Key Vault（機密情報のホスティング）
- バックアップと災害復旧：Azure Backup（データバックアップ）
- DevOps：Azure DevOps（CI/CD パイプライン、コードリポジトリ）

### 3.4 その他ツール

- コードリポジトリ：Azure Repos/GitHub
- テストツール：JMeter（負荷テスト）、JUnit（バックエンド単体テスト）
- セキュリティツール：OWASP Dependency Check（依存パッケージのセキュリティ検出）

## 4. フロントエンドアーキテクチャ

フロントエンドは**極簡素版 MVVM アーキテクチャ**を採用し、複雑な層分けと冗長なコードを廃止し、「View-ViewModel-Model」の核心的な 3 層のみを保留する。これは「App が WebView を主体とし、ビジネスロジックが簡単」なシナリオに合わせると同時に、コードの保守性を保証し、スパゲッティコードを回避する。

### 4.1 React Native App アーキテクチャ（極簡素 MVVM）

#### 4.1.1 アーキテクチャ層分け

1.  View 層（ビュー層）：核心は WebView コンポーネントで、既存保険 Web エンドのコンテンツを読み込む；少数のネイティブページ（e-ninsho 認証ページ、スタンプ抽選ページ、家族連携ページ、設定ページ）を含み、UI レンダリングとユーザー行動の収集（クリック、入力など）のみを担当し、いかなるビジネスロジックも含まない。
2.  ViewModel 層（ビューモデル層）：View と Model の中間層として、軽量な状態（WebView の読み込み状態、e-ninsho 認証状態、スタンプ数、Push 通知状態）を管理し、View 層のユーザー行動を受け取り、Model 層の API 呼び出しをスケジューリングし、処理結果を View 層にフィードバックして UI の更新を駆動する；複雑なビジネスロジックはなく、状態のフローとリクエストスケジューリングのみを担当する。
3.  Model 層（データ層）：API 呼び出し（既存保険 Web エンドインターフェース、バックエンド新規核心インターフェース<家族連携、SSO トークン生成など>）、ローカルストレージ操作（AsyncStorage/MMKV）、e-ninsho SDK 呼び出しをカプセル化し、統一的なデータアクセスエントリーを提供する；リポジトリ、データソースなどの複雑なモジュールの分割は不要で、極簡素な設計とする。

#### 4.1.2 核心モジュール設計

- WebView モジュール：核心モジュールで、既存保険 Web エンドの URL を読み込み、SSO トークンを携帯してシングルサインオンを実現し、WebView とネイティブの簡単な通信（例：ネイティブスタンプページへのジャンプ）を処理する；
- 認証モジュール：e-ninsho SDK を統合し、公共個人認証を実現し、バックエンド認証インターフェースと接続し、ログイン状態と JWT トークンを管理する；
- 家族連携モジュール：バックエンド家族連携インターフェースと接続し、家族の保険アカウントバインディング、SSO トークン取得を実現し、WebView にジャンプして家族の保険契約を閲覧する；
- スタンプ抽選モジュール：バックエンドインターフェースと接続し、ユーザーのスタンプ行動を記録、スタンプ数をクエリし、20 個集めたら抽選機能をロック解除する；
- Push 通知モジュール：Azure Notification Hubs SDK を統合し、Push 通知を受信し、通知クリックイベントを処理（対応する WebView ページにジャンプ）する；
- 設定モジュール：一般的な設定、ログアウトなどの機能を提供し、バックエンドログアウトインターフェースと接続し、ローカルキャッシュとログイン状態をクリアする。

#### 4.1.3 核心設計原則

- 極簡素を優先：核心的な層分けのみを保留し、冗長なモジュールを追加せず、開発と保守コストを低減する；
- WebView 主体：既存保険 Web エンドのコンテンツを最大限に再利用し、ネイティブ開発の工数を削減する；
- 状態の軽量化：必要な状態のみを管理し、状態の冗長性を回避し、デバッグを簡素化する；
- 再利用性：API カプセル化、共通コンポーネント（ボタン、ポップアップ）をグローバルに再利用し、重複開発を削減する。

#### 4.1.4 コードディレクトリ構造

```
src/
├── assets/                  # 静的リソース：画像、フォント、スタイルファイル
│   ├── images/              # 画像リソース（アイコン、背景画像）
│   └── styles/              # グローバルスタイル（テーマ、共通スタイル）
├── components/              # 共通コンポーネント：ボタン、ポップアップ、ローディングボックスなど（グローバル再利用）
│   ├── Button/              # カスタムボタンコンポーネント
│   ├── Loading/             # ローディングコンポーネント
│   └── Modal/               # ポップアップコンポーネント
├── view/                    # View層：ページとコンポーネント（レンダリングのみ、ビジネスロジックなし）
│   ├── webviews/            # WebView核心ページ
│   │   ├── InsuranceWebView.tsx  # 保険契約WebViewページ
│   │   └── BaseWebView.tsx       # 汎用WebViewカプセル化ページ
│   └── native/              # 少数のネイティブインタラクションページ
│       ├── auth/            # e-ninsho認証関連ページ
│       │   └── ENinshoAuthPage.tsx
│       ├── familyLink/      # 家族連携関連ページ
│       │   ├── FamilyBindPage.tsx
│       │   └── FamilyContractPage.tsx
│       ├── stamp/           # スタンプ抽選関連ページ
│       │   ├── StampListPage.tsx
│       │   └── LotteryPage.tsx
│       ├── push/            # Push通知関連ページ
│       │   └── PushDetailPage.tsx
│       └── settings/        # 設定ページ
│           └── SettingsPage.tsx
├── viewModel/               # ViewModel層：軽量な状態とリクエストスケジューリング
│   ├── WebViewVM.ts         # WebView関連状態管理
│   ├── AuthVM.ts            # 認証関連状態管理
│   ├── FamilyLinkVM.ts      # 家族連携関連状態管理
│   ├── StampVM.ts           # スタンプ抽選関連状態管理
│   ├── PushVM.ts            # Push通知関連状態管理
│   └── SettingsVM.ts        # 設定関連状態管理
├── model/                   # Model層：データアクセス（API/ストレージ/SDK）
│   ├── api/                 # APIカプセル化
│   │   ├── index.ts         # APIリクエストインターセプター/レスポンスインターセプターカプセル化
│   │   ├── authApi.ts       # 認証関連API
│   │   ├── familyLinkApi.ts # 家族連携関連API
│   │   ├── stampApi.ts      # スタンプ抽選関連API
│   │   └── pushApi.ts       # Push通知関連API
│   ├── storage/             # ローカルストレージカプセル化
│   │   ├── AsyncStorageUtil.ts
│   │   └── MMKVUtil.ts
│   └── sdk/                 # サードパーティSDKカプセル化
│       ├── ENinshoSDK.ts    # e-ninsho SDKカプセル化
│       └── NotificationHubsSDK.ts # Push SDKカプセル化
├── navigation/              # ルーティング設定：React Navigation
│   └── AppNavigator.tsx
├── utils/                   # 共通ツールクラス
│   ├── request.ts           # ネットワークリクエストツール
│   ├── format.ts            # データフォーマットツール
│   └── device.ts            # デバイス関連ツール
└── types/                   # TSタイプ定義
    ├── auth.types.ts
    ├── familyLink.types.ts
    └── stamp.types.ts
```

### 4.2 React 管理ツール アーキテクチャ（極簡素 MVVM）

#### 4.2.1 アーキテクチャ層分け

1.  View 層（ビュー層）：React コンポーネントとページで、ログインページ、コンテンツ管理ページ（作成、編集、公開）、Push 送信管理ページ（作成、送信、統計）を含み、UI レンダリングとユーザー操作の収集のみを担当し、ViewModel 層の状態に依存して UI を更新する。
2.  ViewModel 層（ビューモデル層）：ページ状態（フォーム状態、リストページング状態、Push 送信状態、ローディング状態）を管理し、View 層の操作指令を受け取り、Model 層の API 呼び出しをスケジューリングし、簡単な状態フローを処理し、複雑なビジネスロジックはない。
3.  Model 層（データ層）：バックエンドインターフェース呼び出し（コンテンツ管理、Push 送信、ログイン認証）をカプセル化し、統一的なデータアクセスエントリーを提供し、ViewMode 層の呼び出しロジックを簡素化する。

#### 4.2.2 核心モジュール設計

- ログイン認証モジュール：バックエンドログインインターフェースと接続し、管理者のログイン、権限検証を実現し、ログイン状態（JWT トークン）を管理する；
- コンテンツ管理モジュール：バックエンドコンテンツインターフェースと接続し、コンテンツの作成、編集、公開、削除を実現し、コンテンツリストのクエリとフィルタリングをサポートする；
- Push 送信管理モジュール：バックエンド Push インターフェースと Azure Notification Hubs と接続し、Push 通知コンテンツの作成、プッシュ範囲の選択（全員/指定ユーザー）、プッシュタスクの提出、プッシュ結果の統計（配信率、クリック率）を実現する；
- システム設定モジュール：簡単なシステム設定（例：Push プッシュパラメータ、コンテンツ審査ルール）を提供する。

#### 4.2.3 核心設計原則

- 管理エンドシナリオに適合：Ant Design Pro コンポーネントライブラリに基づき、開発効率を向上させ、UI の一貫性を保証する；
- 極簡素な層分け：複雑な状態管理と層分けを回避し、開発難度を低減し、チームの技術背景に適合する；
- 高効率なインタラクション：フォーム操作、リストクエリなどの高頻度シナリオを最適化し、運用担当者の作業効率を向上させる。

#### 4.2.4 コードディレクトリ構造

```
src/
├── assets/                  # 静的リソース：画像、スタイル、アイコン
│   ├── images/
│   └── styles/
├── components/              # 共通コンポーネント：テーブル、フォーム、統計カードなど
│   ├── Table/               # 汎用テーブルコンポーネント（ページング、フィルタリング付き）
│   ├── Form/                # 汎用フォームコンポーネント
│   └── StatCard/            # データ統計カードコンポーネント
├── view/                    # View層：ページとコンポーネント（レンダリングのみ）
│   ├── login/               # ログインページ
│   │   └── LoginPage.tsx
│   ├── content/             # コンテンツ管理関連ページ
│   │   ├── ContentListPage.tsx   # コンテンツリストページ
│   │   └── ContentEditPage.tsx   # コンテンツ編集ページ
│   ├── push/                # Push送信管理関連ページ
│   │   ├── PushListPage.tsx      # Pushタスクリストページ
│   │   ├── PushCreatePage.tsx    # Pushタスク作成ページ
│   │   └── PushStatPage.tsx      # Pushデータ統計ページ
│   └── settings/            # システム設定ページ
│       └── SettingsPage.tsx
├── viewModel/               # ViewModel層：状態とリクエストスケジューリング
│   ├── LoginVM.ts           # ログイン関連状態管理
│   ├── ContentVM.ts         # コンテンツ管理関連状態管理
│   ├── PushVM.ts            # Push送信関連状態管理
│   └── SettingsVM.ts        # システム設定関連状態管理
├── model/                   # Model層：データアクセス
│   ├── api/                 # APIカプセル化
│   │   ├── index.ts         # リクエストインターセプター/レスポンスインターセプター
│   │   ├── loginApi.ts      # ログイン関連API
│   │   ├── contentApi.ts    # コンテンツ管理関連API
│   │   └── pushApi.ts       # Push送信関連API
│   └── storage/             # ローカルストレージ（token、ユーザー情報）
│       └── LocalStorageUtil.ts
├── router/                  # ルーティング設定：React Router
│   └── index.tsx
├── utils/                   # 共通ツールクラス
│   ├── request.ts           # ネットワークリクエストツール
│   ├── format.ts            # 時間/データフォーマットツール
│   └── auth.ts              # 権限検証ツール
└── types/                   # TSタイプ定義
    ├── content.types.ts
    └── push.types.ts
```

### 4.3 フロントエンド共通設計

- インターフェースの統一カプセル化：App エンドと管理ツールエンドは Axios リクエストを統一的にカプセル化し、リクエストインターセプター（JWT トークンの追加）、レスポンスインターセプター（統一的なエラー処理、ステータスコードの判断）を処理し、インターフェースタイプ定義（TS）を再利用し、タイプの不一致を回避する；
- コード規範：ESLint+Prettier を採用してコードスタイルを統一し、Git Hooks（husky+lint-staged）を通じてコミットされるコードが規範に符合することを保証する；
- 権限制御：App エンドは JWT トークンに基づいてログイン権限制御を実現し、管理ツールエンドはページ、ボタンレベルの簡単な権限制御を実現する；
- 互換性：App エンドは iOS 14.0+、Android 8.0+をサポート；管理ツールエンドは Chrome、Firefox、Edge などの主流ブラウザの最新版から 2 つ前のバージョンまでをサポート。

## 5. バックエンドアーキテクチャ

バックエンドは**軽量版 Clean Architecture**を採用し、純粋な Clean Architecture の複雑な内部層設計を廃止し、「インターフェース層-ビジネスサービス層-データアクセス層」の核心的な 3 層のみを保留する。家族連携、SSO トークン生成など少数の核心ビジネスロジックに焦点を当て、既存保険 Web エンドのインターフェースリソースを再利用し、開発と落地の難度を簡素化すると同時に、コードの保守性と拡張性を保証する。

### 5.1 アーキテクチャ層分け

#### 5.1.1 インターフェース層（Controller）

- 核心責任：フロントエンド（App エンド、管理ツールエンド）のリクエストを受け取り、パラメータ検証（リクエストボディ、リクエストパラメータの合法性検証）を行い、ビジネスサービス層のメソッドを呼び出し、標準化されたレスポンス（統一的な成功/失敗フォーマット、ステータスコード）を返却する；
- 核心モジュール：家族連携インターフェースモジュール、SSO トークンインターフェースモジュール、コンテンツ管理インターフェースモジュール、Push 送信インターフェースモジュール、ログイン認証インターフェースモジュール；
- 設計原則：いかなるビジネスロジックも含まず、リクエストの受け取りとレスポンスの返却のみを担当し、インターフェースの命名規範、パスが明確で、RESTful API 設計規範に適合する。

#### 5.1.2 ビジネスサービス層（Service）

- 核心責任：システムの核心ビジネスロジック処理で、家族連携、SSO トークン生成、Push プッシュスケジューリング、コンテンツ管理など少数の必要なビジネスロジックのみを実現し、既存保険 Web エンドのインターフェースリソースを再利用する；
- 核心モジュール：
  1.  家族連携サービス：家族の保険アカウントバインディング検証、親族関係の検証、家族権限制御を実現する；
  2.  SSO サービス：SSO トークンの生成、検証、有効期限切れ管理を実現し、App エンドから既存保険 Web エンドへのシングルサインオンをサポートする；
  3.  Push サービス：Azure Notification Hubs と接続し、Push 通知のスケジューリング、バッチプッシュ、プッシュ結果の統計を実現する；
  4.  コンテンツサービス：コンテンツの作成、編集、公開、削除の簡単なビジネスロジックを実現し、管理ツールエンドの操作をサポートする；
  5.  認証サービス：App エンドユーザー、管理ツールエンド管理者のログイン認証、JWT トークンの生成と検証を実現する；
- 設計原則：ビジネスロジックが簡潔で明確で、複雑なトランザクションと冗長なコードを回避する；インターフェース+実装クラスの方式を採用し、後続の拡張を容易にする；核心ビジネスロジックにはログ記録を行い、障害の特定を容易にする。

#### 5.1.3 データアクセス層（Repository）

- 核心責任：データの永続化操作を担当し、Azure SQL Database、Azure Cache for Redis、Azure Blob Storage などのデータストレージコンポーネントと接続し、統一的なデータアクセスエントリーを提供する；
- 核心モジュール：家族連携データアクセスモジュール、ユーザーデータアクセスモジュール、コンテンツデータアクセスモジュール、Push プッシュデータアクセスモジュール、SSO トークンデータアクセスモジュール；
- 設計原則：いかなるビジネスロジックも含まず、CRUD 操作のみを担当する；MyBatis-Plus に基づいてコード開発を簡素化し、高頻度クエリシナリオに対してインデックスを追加する；Azure サービスとの接続には SDK 呼び出しを採用し、データアクセスロジックを簡素化する。

#### 5.1.4 基礎サポート層（Common/Config）

- 核心責任：グローバルな汎用的なサポート能力を提供し、他の 3 層にサービスを提供する；
- 核心モジュール：
  1.  汎用コンポーネント：例外処理（グローバル例外ハンドラー）、レスポンス結果のカプセル化（標準化されたレスポンス）、ツールクラス（時間、暗号化、UUID 生成など）、定数定義；
  2.  設定モジュール：Azure サービス設定（App Service、SQL、Notification Hubs など）、Redis 設定、JWT 設定、クロスドメイン設定（Front Door によって統一的に処理され、バックエンドは補助的な設定のみを行う）；
  3.  セキュリティモジュール：機密情報の暗号化、権限検証補助、e-ninsho SDK 統合設定；
- 設計原則：汎用化、標準化を実現し、重複開発を回避し、システムの各モジュールの正常な運行をサポートする。

#### 5.1.5 コードディレクトリ構造

```
src/main/java/com/insurance/
├── controller/              # インターフェース層：リクエストの受け取り、レスポンスの返却
│   ├── AuthController.java      # 認証関連インターフェース
│   ├── FamilyLinkController.java # 家族連携関連インターフェース
│   ├── SSOController.java       # SSOトークン関連インターフェース
│   ├── ContentController.java   # コンテンツ管理関連インターフェース
│   └── PushController.java      # Push送信関連インターフェース
├── service/                 # ビジネスサービス層：核心ビジネスロジック
│   ├── impl/                # サービス実装クラス
│   │   ├── AuthServiceImpl.java
│   │   ├── FamilyLinkServiceImpl.java
│   │   ├── SSOServiceImpl.java
│   │   ├── ContentServiceImpl.java
│   │   └── PushServiceImpl.java
│   ├── AuthService.java     # 認証サービスインターフェース
│   ├── FamilyLinkService.java # 家族連携サービスインターフェース
│   ├── SSOService.java      # SSOサービスインターフェース
│   ├── ContentService.java  # コンテンツ管理サービスインターフェース
│   └── PushService.java     # Push送信サービスインターフェース
├── repository/              # データアクセス層：CRUD操作
│   ├── impl/                # リポジトリ実装クラス
│   │   ├── UserRepositoryImpl.java
│   │   ├── FamilyLinkRepositoryImpl.java
│   │   ├── ContentRepositoryImpl.java
│   │   └── PushRepositoryImpl.java
│   ├── UserRepository.java  # ユーザーデータアクセスインターフェース
│   ├── FamilyLinkRepository.java # 家族連携データアクセスインターフェース
│   ├── ContentRepository.java # コンテンツデータアクセスインターフェース
│   └── PushRepository.java  # Pushデータアクセスインターフェース
├── model/                   # データモデル
│   ├── entity/              # データベースエンティティクラス
│   │   ├── User.java
│   │   ├── FamilyLink.java
│   │   ├── Content.java
│   │   └── PushTask.java
│   └── dto/                 # フロントエンドとバックエンドのインタラクションDTO
│       ├── request/         # リクエストDTO
│       │   ├── AuthRequest.java
│       │   └── FamilyBindRequest.java
│       └── response/        # レスポンスDTO
│           ├── BaseResponse.java
│           └── StampResponse.java
├── common/                  # 基礎サポート層-汎用コンポーネント
│   ├── exception/           # 例外処理
│   │   ├── BusinessException.java
│   │   └── GlobalExceptionHandler.java
│   ├── util/                # 汎用ツールクラス
│   │   ├── DateUtil.java
│   │   ├── EncryptUtil.java
│   │   └── RedisUtil.java
│   └── constant/            # 定数定義
│       ├── AuthConstants.java
│       └── RedisConstants.java
├── config/                  # 基礎サポート層-設定モジュール
│   ├── AzureConfig.java     # Azureサービス設定
│   ├── RedisConfig.java     # Redis設定
│   ├── SecurityConfig.java  # セキュリティ設定（JWT/Spring Security）
│   └── WebConfig.java       # Web設定（クロスドメインなど）
└── Application.java         # Spring Boot起動クラス

src/main/resources/
├── application.yml          # グローバル設定ファイル
├── application-dev.yml      # 開発環境設定
├── application-test.yml     # テスト環境設定
├── application-prod.yml     # 本番環境設定
├── mybatis/                 # MyBatisマッピングファイル
│   ├── UserMapper.xml
│   └── FamilyLinkMapper.xml
└── static/                  # 静的リソース（予備）
```

### 5.2 核心ビジネスフローのサポート

- e-ninsho 認証サポート：バックエンドは App エンドから渡された e-ninsho 認証トークンを受け取り、e-ninsho 公式インターフェースを呼び出してトークンの有効性を検証し、検証通過後にシステム JWT トークンを生成して App エンドに返却し、ログイン状態のバインディングを完了する；
- 家族連携サポート：バックエンドは App エンドの家族バインディングリクエストを受け取り、ユーザーと家族の親族関係および保険アカウントの関連性を検証し、バインディング成功後にバインディング関係をストレージする；App エンドの SSO トークンリクエストを受け取り、バインディング関係の有効性を検証した後、短時間有効な SSO トークンを生成し、App エンドが既存保険 Web エンドにジャンプすることをサポートする；
- Push プッシュサポート：バックエンドは管理ツールエンドの Push プッシュタスクを受け取り、Azure Notification Hubs をスケジューリングしてバッチプッシュ（1 バッチあたり 1 万人のユーザー、限流を回避）を行い、プッシュログを記録し、プッシュ結果（配信率、クリック率）を統計し、管理ツールエンドのクエリに供する；
- コンテンツ管理サポート：バックエンドは管理ツールエンドのコンテンツ操作リクエストを受け取り、コンテンツの永続化ストレージを完了し、必要に応じて Blob Storage に同期更新（画像/動画のストレージが必要な場合）し、App エンドの WebView が読み込んで表示することを供する。

### 5.3 設計原則

1.  軽量かつ簡潔：複雑な層分けと冗長なコードを廃止し、必要なビジネスロジックのみを実現し、開発と落地の難度を低減し、チームの技術背景に適合する；
2.  再利用を優先：既存保険 Web エンドのインターフェースリソースを最大限に再利用し、バックエンド開発の工数を削減する；
3.  保守性：核心ビジネスロジックとデータアクセスを分離し、インターフェースと実装を分離し、後続の障害特定と機能拡張を容易にする；
4.  高可用性への適合：Azure のデュアルアクティブサービス（プライマリ・セカンダリ App Service、プライマリ・セカンダリ SQL）と接続し、ビジネスサービスの継続的な可用性を確保する；
5.  セキュリティとコンプライアンス：保険業務のニーズに合わせ、機密データの暗号化、権限制御、ログ監査を実現し、データセキュリティとコンプライアンスを保障する。

## 6. インフラストラクチャとセキュリティ

本システムのインフラストラクチャは完全に Microsoft Azure クラウドサービスに基づいて構築され、「高可用性、高セキュリティ、運用容易性」に焦点を当て、保険業務のセキュリティニーズと結合し、Azure の各種セキュリティサービスを統合し、完整なインフラストラクチャとセキュリティ保護システムを形成する。

### 6.1 Azure インフラストラクチャのデプロイ

#### 6.1.1 ゲートウェイとアクセス層

- コンポーネント：Azure Front Door Premium + WAF
- デプロイ設定：
  1.  ルーティングルール：リクエストパスに応じて対応するサービスに転送し、App エンドのリクエストを App Service（プライマリ/セカンダリ）に転送し、管理ツールエンドの静的リソースリクエストを Azure Static Web Apps に転送し、静的/動画リソースリクエストを Blob Storage+CDN に転送する；
  2.  WAF 設定：OWASP Top 10 保護ルールを有効にし、SQL インジェクション、XSS、CSRF などの悪意のあるリクエストを遮断する；高頻度リクエスト制限（単一 IP は 1 分間に ≤100 回）を設定し、e-ninsho 認証コールバック IP を許可する；リクエストボディのサイズを ≤10MB に制限し、インターフェースの乱用を防止する；
  3.  ヘルスチェック：バックエンドが提供するヘルスチェックインターフェースを設定し、チェック間隔は 30 秒、連続 3 回の失敗で自動的にトラフィックをセカンダリインスタンスに切り替える；
  4.  キャッシュ設定：静的リソース（画像、CSS、JS）のキャッシュ TTL=1 時間、インターフェースの非機密レスポンスのキャッシュ TTL=5 分、バックエンドサービスの負荷を低減する。

#### 6.1.2 アプリケーションサービス層

- コンポーネント：App Service（プライマリインスタンス、セカンダリインスタンス）
- デプロイ設定：
  1.  価格レベル：プライマリインスタンスは Standard S3（4 コア 8G）を採用し、日間アクティブユーザー 10 万人の同時実行ニーズに対応；セカンダリインスタンスは Standard S2（2 コア 4G）を採用し、災害復旧とトラフィック分担に使用；
  2.  弾性スケーリング：プライマリインスタンスは自動スケーリングルールを有効にし、CPU 使用率>70%の場合にインスタンスを増やし（最大 4 個）、CPU 使用率<30%の場合にインスタンスを減らし、トラフィックの変動に対応；セカンダリインスタンスはプライマリインスタンスと設定を同期し、プライマリインスタンスに障害が発生した場合にのみ有効にする；
  3.  デプロイモード：ブルーグリーンデプロイ戦略を採用し、デプロイプロセス中のサービス中断を回避し、バージョンロールバックを容易にする；
  4.  ネットワーク設定：Azure Virtual Network（VNet）に加入し、Front Door からのアクセスのみを許可し、パブリックネットワークからの直接アクセスを禁止し、セキュリティを向上させる；
  5.  ログ設定：アプリケーションログ、HTTP ログをオンにし、Log Analytics に出力し、障害の特定と分析を容易にする。

#### 6.1.3 データストレージ層

1.  Azure SQL Database（プライマリデータベース、セカンダリデータベース）

    - デプロイ設定：いずれも General Purpose 価格レベル（GP_Gen5_4、4 コア 16G）を採用し、クロスリージョンレプリケーション機能を有効にし、プライマリ・セカンダリデータベースのデータリアルタイム同期を実現する；
    - 高可用性設定：自動フェイルオーバーを有効にし、障害発生後 1 分以内に自動的にセカンダリデータベースをプライマリデータベースに昇格させ、データの継続性を保障する；
    - インデックス設定：ユーザーテーブル、家族バインディングテーブル、コンテンツテーブル、スタンプテーブルなどの核心テーブルに対し、高頻度クエリシナリオに基づいてインデックスを作成し、クエリパフォーマンスを最適化する；
    - セキュリティ設定：Defender for Cloud 保護を有効にし、SQL インジェクション攻撃、弱いパスワードなどのセキュリティリスクを検出する；データ暗号化（転送+ストレージ）を有効にし、データセキュリティを保障する。

2.  Azure Blob Storage（汎用+動画）

    - デプロイ設定：2 つのコンテナを作成し、それぞれ汎用静的リソース（ホットストレージレベル）、動画リソース（コールドストレージレベル）のストレージに使用する；
    - 権限設定：汎用静的リソースコンテナはプライベート権限に設定し、バックエンドサービスと Front Door からのアクセスのみを許可する；動画リソースコンテナは匿名読み取り権限を有効にし、CDN を介して加速アクセスする；
    - 冗長設定：クロスリージョン冗長ストレージを有効にし、リージョン障害時のリソースの回復可能性を保障する；
    - 暗号化設定：Azure 自動暗号化を有効にし、転送プロセスは HTTPS 暗号化を採用し、ストレージプロセスは Azure 組み込みの暗号化アルゴリズムを採用する。

3.  Azure Cache for Redis
    - デプロイ設定：Standard 価格レベル、マスタースレーブアーキテクチャを採用し、SSO トークン、ユーザーログイン状態、高頻度クエリデータ（スタンプ数など）のストレージに使用する；
    - キャッシュ戦略：SSO トークンのキャッシュ有効期間は 10 分、高頻度クエリデータのキャッシュ有効期間は 5 分、キャッシュの冗長性とデータの不一致を回避する；
    - セキュリティ設定：パスワード認証を有効にし、アクセス IP を制限し、App Service からのアクセスのみを許可し、キャッシュセキュリティを保障する。

#### 6.1.4 メッセージプッシュ層

- コンポーネント：Azure Notification Hubs
- デプロイ設定：
  1.  プッシュ設定：iOS/Android プッシュ証明書を Azure Key Vault にアップロードし、バックエンドは SDK を介して動的に取得し、プッシュチャネルを設定する；
  2.  プッシュ戦略：バッチプッシュ（1 バッチあたり 1 万人のユーザー）、リトライ回数は 3 回、プッシュの失敗を回避する；プッシュログを有効にし、Log Analytics に同期する；
  3.  セキュリティ設定：アクセスキー認証を有効にし、バックエンドサービスからのプッシュインターフェースの呼び出しのみを許可し、乱用を防止する。

#### 6.1.5 バックアップとディザスタリカバリ層

- コンポーネント：Azure Backup
- デプロイ構成：
  1.  バックアップ範囲：Azure SQL Database（プライマリデータベース＋セカンダリデータベース）、Blob Storage、App Service 設定、バックエンドコード設定；
  2.  バックアップポリシー：毎日午前 2 時にフルバックアップを実行、4 時間ごとに増分バックアップを実行、バックアップ保有期間は 30 日間；
  3.  リストア構成：任意の時点でのリストア（30 日間以内）をサポート、リストアテストプロセスを構成、毎月リストア演習を実施し、バックアップの有効性を保障する。

### 6.2 セキュリティシステム設計

#### 6.2.1 ネットワークセキュリティ

- 境界防御：Azure Front Door Premium + WAF によりネットワーク境界防御を構築、悪意のあるリクエストを遮断、不正アクセスを制限；
- ネットワーク分離：すべてのコアサービス（App Service、SQL、Redis）を VNet に加入、ネットワークセキュリティグループ（NSG）を構成、必要なポート（443/80）のみを開放、パブリックネットワークからの直接アクセスを禁止；
- トランスポート暗号化：クライアントと Front Door、Front Door と App Service、App Service と SQL/Redis/Blob Storage 間の通信を含む、すべてのネットワークトランスポートに HTTPS 暗号化を採用、データ転送中の漏洩を回避。

#### 6.2.2 データセキュリティ

- ストレージ暗号化：Azure SQL、Blob Storage、Redis はいずれも Azure 自動暗号化機能を有効にし、データを静止時に暗号化し、データストレージの安全性を保障；
- 機微情報管理：データベースパスワード、JWT キー、Push 証明書、e-ninsho API キーなどのすべての機微情報を Azure Key Vault に托管、バックエンドは SDK を介して動的に取得し、ハードコーディングを回避；
- データ仮名化：保険契約、ユーザー本人確認情報などの機微データをバックエンドから返却する際、身分証番号、携帯電話番号などの機微フィールドを仮名化処理し、機微情報の漏洩を回避；
- データアクセス制御：Azure RBAC（役割ベースのアクセス制御）に基づき、各開発者・運用管理者に最小限の権限を割り当て、担当する環境とサービスへのアクセスのみを許可；保険契約、ユーザー認証情報などのコアデータへのアクセス操作はすべて監査ログに記録。

#### 6.2.3 アプリケーションセキュリティ

- 認証認可：JWT ステートレス認証を採用、App 端末、管理ツール端末のログイン認証を実現；家族連携機能によりきめ細かい権限制御を実現、紐づけられた家族の保険契約の閲覧のみを許可；
- 入力検証：すべてのフロントエンドリクエストはバックエンドでパラメータ検証を経て、不正なパラメータインジェクション、SQL インジェクションなどの攻撃を防止；
- SDK セキュリティ：e-ninsho SDK に署名検証を実施、SDK の改ざんを防止；定期的に SDK バージョンを更新し、セキュリティ脆弱性を修復；
- 脆弱性防御：Defender for Cloud によりクラウドリソース、アプリケーション依存パッケージを定期的にスキャン、セキュリティ脆弱性を検出、修復提案を提供、高危険度脆弱性を速やかに修復。

#### 6.2.4 セキュリティ監視と監査

- セキュリティ監視：Microsoft Sentinel により WAF ログ、SQL 監査ログ、アプリケーションセキュリティログなどのすべてのセキュリティログを統合、異常なログイン、機微データの一括アクセス、インターフェース失敗率の異常などのセキュリティアラートルールを構成、アラート発生後はメール、SMS により運用管理者に通知をプッシュ；
- ログ監査：ユーザーログイン、e-ninsho 認証、保険契約閲覧、Push プッシュ、コンテンツ管理などのすべてのコア操作について、ユーザー ID、操作時間、IP アドレス、操作結果を含む詳細なログを記録、ログ保有期間は 90 日間、セキュリティ監査と障害追跡を容易にする；
- コンプライアンスチェック：Defender for Cloud によりコンプライアンスチェックを実施、保険業界のデータセキュリティコンプライアンス要求に適合、非コンプライアンス項目を速やかに是正。

## 7. DevOps & CI/CD（Azure DevOps に基づく）

チームの技術背景とプロジェクトの特徴を結びつけ、Azure DevOps を採用して完全な DevOps システムを構築、コード管理、継続的インテグレーション（CI）、継続的デプロイ（CD）、環境管理の全プロセス自動化を実現、開発効率を向上、デプロイ品質を保障、日間アクティブユーザー 10 万人規模のデプロイニーズに適応する。

### 7.1 コード管理

- コードリポジトリ：Azure Repos（Git）をコードリポジトリとして採用、モジュール別にコードディレクトリ（App フロントエンド、管理ツールフロントエンド、バックエンドサービス）を划分し、コードの分離と統合管理を実現；
- ブランチ管理戦略：
  1.  メインブランチ（main）：運用環境のコードのみを保存、直接コミットを禁止、開発ブランチ・テストブランチからのマージリクエスト（MR）を通じてのみマージを許可；
  2.  開発ブランチ（develop）：開発環境のコードを保存、すべての開発者が開発ブランチでコード開発とコミットを実施；
  3.  テストブランチ（test）：開発ブランチからコードをマージ、テスト環境へのデプロイとテストに使用、テスト合格後にメインブランチにマージ；
  4.  機能ブランチ（feature/\*）：新機能開発時に開発ブランチから機能ブランチを作成、機能開発完了後に開発ブランチにマージ、機能ブランチを削除；
- コード品質管理：Git Hooks（husky+lint-staged）を構成、コードコミット前に自動的に ESLint、Prettier によるコードチェックを実行、コード規範への準拠を確保；バックエンドコードには Java コード規範チェック（Alibaba Java Coding Guidelines）を実行、非準拠コードのコミットを禁止。

### 7.2 継続的インテグレーション（CI）パイプライン

Azure DevOps Pipelines に基づき CI パイプラインを構築、コードコミット後の自動ビルド、テスト、パッケージングを実現、コード品質を確保、パイプラインはフロントエンド（App+管理ツール）、バックエンドの 2 本の独立したパイプラインに分かれ、同時実行する。

#### 7.2.1 フロントエンド CI パイプライン（App+管理ツール）

1.  トリガー条件：開発ブランチ、テストブランチ、メインブランチにコードコミットまたはマージリクエストがある場合、自動的にパイプラインをトリガー；
2.  コアステップ：
    1.  コード取得：Azure Repos から最新のコードを取得；
    2.  依存関係インストール：npm install を実行してフロントエンド依存パッケージをインストール、依存パッケージをキャッシュし、ビルド速度を向上；
    3.  コードチェック：ESLint、Prettier によりコード規範をチェック、チェック失敗時はパイプラインを終了；
    4.  ユニットテスト：フロントエンドユニットテスト（Jest）を実行、テストカバレッジは 60%以上に達する必要があり、そうでない場合はパイプラインを終了；
    5.  ビルドとパッケージング：App 端末では EAS Build を実行してパッケージングし、iOS/Android インストーラーパッケージを生成；管理ツール端末では Vite を実行してビルドし、静的リソースパッケージを生成；
    6.  成果物アップロード：ビルド成果物（インストーラーパッケージ、静的リソースパッケージ）を Azure Artifacts にアップロード、CD パイプラインのデプロイソースとする。

#### 7.2.2 バックエンド CI パイプライン

1.  トリガー条件：フロントエンドパイプラインと同様、開発ブランチ、テストブランチ、メインブランチにコードコミットまたはマージリクエストがある場合にトリガー；
2.  コアステップ：
    1.  コード取得：Azure Repos から最新のバックエンドコードを取得；
    2.  依存関係インストール：Maven を介してバックエンド依存パッケージをインストール、依存パッケージをキャッシュ；
    3.  コードチェック：Java コード規範チェック、依存パッケージセキュリティ検出（OWASP Dependency Check）を実行、チェック失敗時はパイプラインを終了；
    4.  ユニットテスト：JUnit ユニットテストを実行、テストカバレッジは 70%以上に達する必要があり、そうでない場合はパイプラインを終了；
    5.  パッケージングとビルド：Maven を介してパッケージングし、Spring Boot 実行可能 JAR パッケージを生成；
    6.  成果物アップロード：JAR パッケージを Azure Artifacts にアップロード、CD パイプラインのデプロイソースとする。

### 7.3 継続的デプロイ（CD）パイプライン

Azure DevOps Pipelines に基づき CD パイプラインを構築、ビルド成果物の自動デプロイを実現、環境（開発、テスト、運用）別にフェーズ分けしてデプロイし、デプロイプロセスの安全性と制御性を確保、App Service のプライマリ・セカンダリデュアルライブデプロイニーズに適応する。

#### 7.3.1 環境管理

- 環境划分：開発環境、テスト環境、運用環境に分け、3 つの環境は独立した Azure リソースグループ（Resource Group）を採用、環境の分離を実現、相互影響を回避；
  1.  開発環境：開発者の日常デバッグに使用、デプロイ構成を簡略化、リソース仕様を低く設定；
  2.  テスト環境：機能テスト、パフォーマンステスト、セキュリティテストに使用、デプロイ構成は運用環境と一致；
  3.  運用環境：エンドユーザー向け、App Service、SQL のプライマリ・セカンダリデュアルライブデプロイを採用、高可用性を保障、リソース仕様は日間アクティブユーザー 10 万人規模に適応。

#### 7.3.2 デプロイ戦略

- 開発/テスト環境：自動デプロイ戦略を採用、CI パイプラインのビルド成功後、自動的に CD パイプラインをトリガー、成果物を対応する環境にデプロイ；
- 運用環境：ブルーグリーンデプロイ戦略を採用、デプロイ中のサービス中断を回避、コアステップ：
  1.  まず新版本を App Service Secondary（セカンダリインスタンス）にデプロイ、デプロイ完了後にスモークテストを実行、サービスの可用性を検証；
  2.  スモークテスト合格後、Azure Front Door を介してトラフィックを段階的に Secondary インスタンスに切り替え、サービスの運行状態を監視；
  3.  サービスの正常運行を確認後、新版本を App Service Primary（プライマリインスタンス）にデプロイ、スモークテストを実行；
  4.  Primary インスタンスのデプロイ完了と正常性の検証後、トラフィックを Primary インスタンスに切り戻し、Secondary インスタンスを災害復旧用として保留、新版本を保持し、ロールバックを容易にする；
  5.  デプロイ完了後、ヘルスチェックとパフォーマンステストを実行、システムの正常運行を確保。

#### 7.3.3 コアデプロイステップ

1.  成果物取得：Azure Artifacts から CI パイプラインで生成されたビルド成果物（App インストーラーパッケージ、管理ツール静的リソースパッケージ、バックエンド JAR パッケージ）を取得；
2.  デプロイ準備：対象環境の Azure リソース（App Service、SQL、Blob Storage）の運行状態を確認、環境の可用性を確認；
3.  バックエンドデプロイ：JAR パッケージを App Service（Primary/Secondary）にデプロイ、サービスを再起動、ヘルスチェックを実行；
4.  フロントエンドデプロイ：管理ツール静的リソースパッケージを Azure Static Web Apps にデプロイ；App インストーラーパッケージを Azure DevOps にデプロイ、テスター、運用担当者がダウンロードしたり、アプリストアに提出したりするために供する；
5.  設定更新：データベース接続、Azure サービス設定などの環境設定を同期更新、システムの正常運行を確保；
6.  テスト検証：スモークテスト、インターフェーステストを実行、デプロイ後の機能可用性を検証；運用環境では追加でパフォーマンステストを実行、トラフィックニーズへの適応を確認；
7.  ログ記録：デプロイプロセス、デプロイバージョン、デプロイ結果を記録、後続の追跡とロールバックを容易にする。

### 7.4 ロールバック戦略

- トリガー条件：デプロイ後に機能異常、パフォーマンスボトルネック、サービス利用不可などの問題が発生した場合、ロールバックをトリガー；
- ロールバックプロセス：
  1.  運用環境：Azure Front Door を介してトラフィックを新版本がデプロイされていないインスタンス（ブルーグリーンデプロイの予備インスタンス）に迅速に切り替え、サービスの正常運行を回復；
  2.  ロールバックパイプラインを実行、ビルド成果物を前の安定バージョンにロールバック、対応する App Service インスタンスにデプロイ；
  3.  ロールバック完了後、スモークテスト、インターフェーステストを実行、サービスの正常回復を確認；
  4.  ロールバックの原因、プロセス、結果を記録、後続で問題を分析し最適化する。

### 7.5 DevOps ツール統合

- コード管理：Azure Repos + Git
- パイプラインビルド：Azure DevOps Pipelines
- 成果物管理：Azure Artifacts
- テストツール：JUnit（バックエンド）、Jest（フロントエンド）、JMeter（負荷テスト）
- 脆弱性スキャン：OWASP Dependency Check、Defender for Cloud
- 監視アラート：Application Insights、Microsoft Sentinel（デプロイプロセスのアラート）

## 8. 監視と拡張性

本システムは Azure クラウドサービスの監視ツールに基づき、全リンク、多次元の監視システムを構築、パフォーマンス、ログ、セキュリティのリアルタイム監視を実現；同時に日間アクティブユーザー 10 万人規模に対応し、トラフィックの変動、業務拡張のニーズに適応する拡張可能なアーキテクチャを設計、システムの安定運行を保障する。

### 8.1 監視システム設計

#### 8.1.1 パフォーマンス監視（Application Insights に基づく）

- 監視範囲：フロントエンド（App 端末、管理ツール端末）、バックエンドサービス、Azure クラウドサービスの全リンクパフォーマンス監視を実現；
  1.  フロントエンドパフォーマンス監視：App 端末の WebView 読み込み速度、ネイティブページレンダリング速度、インターフェースリクエスト時間、フロントエンド異常（JS エラー、コンポーネントレンダリングエラー）、ユーザーインタラクション遅延を監視；管理ツール端末のページ読み込み速度、フォーム送信速度、インターフェースリクエスト時間、フロントエンド異常を監視；
  2.  バックエンドパフォーマンス監視：インターフェースレスポンス時間、リクエストスループット（QPS）、CPU 使用率、メモリ使用率、データベースクエリ時間、Redis キャッシュヒット率、Azure サービス呼び出し時間（Notification Hubs、Blob Storage）を監視；
  3.  クラウドサービスパフォーマンス監視：App Service（プライマリ・セカンダリインスタンス）の CPU、メモリ、ディスク使用率、ネットワーク帯域幅を監視；Azure SQL Database のクエリパフォーマンス、接続数、ロック待ち時間を監視；Redis キャッシュのメモリ使用率、ヒット率、レスポンス時間を監視；Front Door のルーティング遅延、リクエスト成功率を監視。
- 監視指標と閾値（日間アクティブユーザー 10 万人に適応）：
  1.  インターフェースレスポンス時間：平均レスポンス時間 ≤500ms、95 パーセンタイルのレスポンス時間 ≤1s、2s を超えた場合は低速リクエストとしてマークし、アラートをトリガー；
  2.  リクエストスループット：ピーク時の QPS≤1000、閾値を超えた場合はエラスティックスケーリングをトリガー；
  3.  エラー率：インターフェースエラー率 ≤0.5%、フロントエンド異常率 ≤0.1%、閾値を超えた場合はアラートをトリガー；
  4.  リソース使用率：App Service の CPU 使用率 ≤70%、メモリ使用率 ≤80%；SQL の接続数 ≤500；Redis のメモリ使用率 ≤85%；
- アラート戦略：段階的なアラート（警告、重大）を構成、メール、SMS によりアラート情報を運用管理者にプッシュ；低速リクエスト、エラー率の異常、リソース使用率の超過は警告アラートをトリガー；サービス利用不可、データベース障害、キャッシュペネトレーションは重大アラートをトリガーし、運用管理者に即時通知する。

#### 8.1.2 ログ分析（Log Analytics + Microsoft Sentinel に基づく）

- ログ収集範囲：
  1.  アプリケーションログ：フロントエンド（App 端末、管理ツール端末）の操作ログ、エラーログ、インターフェースリクエストログ；バックエンドサービスの業務ログ、異常ログ、インターフェース呼び出しログ、SQL 実行ログ；
  2.  クラウドサービスログ：App Service ログ、SQL 監査ログ、Front Door ログ、WAF 防御ログ、Notification Hubs プッシュログ、Blob Storage アクセスログ、Redis ログ；
  3.  セキュリティログ：ユーザーログインログ、e-ninsho 認証ログ、機微データアクセスログ、権限検証ログ、WAF 遮断ログ；
- ログ管理：
  1.  ログストレージ：すべてのログを Log Analytics に統合収集、ログタイプ別に分類してストレージ、ログ保有期間は 90 日間、保有期間を超えた場合は自動的に Azure Blob Storage（コールドストレージ）にアーカイブ、アーカイブ保有期間は 180 日間；
  2.  ログ分析：Log Analytics のカスタムクエリにより、インターフェース失敗の原因、ユーザー操作行動、Push プッシュ効果（配信率、クリック率）、スタンプコレクション抽選データ、セキュリティイベントを分析；Microsoft Sentinel によりセキュリティログを統合、セキュリティ脅威を分析、異常行動（異なる地域からのログイン、悪意のある攻撃）を識別；
- ログ可視化：Azure Dashboard によりログ分析ダッシュボードを作成、インターフェースエラー率、Push 配信率、セキュリティイベント数などのコアログ指標をリアルタイムに表示、運用管理者が迅速に閲覧・分析できるようにする。

#### 8.1.3 業務監視

- コア業務監視指標：
  1.  ユーザー関連：日間アクティブユーザー数、ログイン成功率、e-ninsho 認証成功率、家族連携紐付け数；
  2.  業務関連：スタンプ数、抽選回数、当選率、SSO ジャンプ成功率、保険契約閲覧回数；
  3.  管理端末関連：コンテンツ作成数、コンテンツ発行数、Push プッシュ回数、Push 配信率、Push クリック率；
- 監視実現：バックエンドの業務ログによりコア業務指標を統計、Log Analytics に同期、業務監視ダッシュボードを作成、業務の運行状態をリアルタイムに表示；ログイン成功率 ≤95%、Push 配信率 ≤80%などの業務指標異常アラートを構成、業務異常を速やかに発見する。

### 8.2 拡張性設計（日間アクティブユーザー 10 万人に適応、将来の拡張をサポート）

#### 8.2.1 トラフィック拡張性（日間アクティブユーザー 10 万人及びトラフィック変動への対応）

1.  エラスティックスケーリング：App Service（Primary インスタンス）に自動スケーリングルールを有効にし、CPU 使用率、リクエストスループットに基づきインスタンス数を自動的に増減、ピーク時に最大 4 インスタンスに拡張、低谷時に 1 インスタンスに削減、トラフィック変動に適応；Secondary インスタンスは予備として、ニーズに応じて手動で拡張可能；
2.  キャッシュ最適化：スタンプ数クエリ、ユーザー情報クエリ、コンテンツリストクエリなどの高頻度クエリシナリオに対し、Redis キャッシュによりデータベースの負荷を低減；静的リソース、動画リソースは Azure CDN+Front Door によりキャッシュ、オリジンサーバーへのリクエストを減少；
3.  インターフェース最適化：コアインターフェースにはページングクエリ、非同期処理（Push プッシュ、スタンプ記録更新など）を採用、インターフェースのブロッキングを減少；高頻度インターフェースを Gzip で圧縮、データ転送量を減少；
4.  データベース拡張：Azure SQL Database はオンデマンドでの拡張（CPU、メモリ、ストレージ）をサポート、将来的にユーザー規模が 10 万人を超えて増加した場合、価格レベルを直接アップグレード、インスタンス仕様を増やすことができ、コードの修正は不要；
5.  Push プッシュ最適化：バッチプッシュ戦略（1 バッチ 1 万人）を採用、Notification Hubs の流量制限を回避；将来的にユーザー規模が増加した場合、プッシュバッチ数を増やすか、Notification Hubs の価格レベルをアップグレードし、プッシュ能力を向上。

#### 8.2.2 業務拡張性（将来の業務拡張をサポート）

1.  フロントエンド拡張性：コンポーネント化、モジュール化設計を採用、App 端末に新しいネイティブ機能を追加する際、既存のミニマム MVVM アーキテクチャに基づき新しいモジュールを迅速に開発、共通コンポーネントと API カプセル化を再利用；管理ツール端末に新機能を追加する際、既存のページテンプレート、状態管理ロジックを再利用、迅速に拡張；
2.  バックエンド拡張性：ライトウェイトな層別アーキテクチャを採用、インターフェース層、業務サービス層、データアクセス層を分離、新しい業務ロジックを追加する際、新しいインターフェース、サービス、データアクセスモジュールを迅速に開発、既存のコードの修正は不要；コア業務ロジックはフレームワーク、データソースとデカップリング、後続の置換または拡張を容易にする；
3.  クラウドサービス拡張性：Azure クラウドサービスはオンデマンドでの拡張をサポート、将来的に多言語サポート、サードパーティ統合などの新機能を追加する際、Azure Translator、Azure Event Hub などの Azure 関連サービスを迅速に統合、インフラストラクチャの再構築は不要；
4.  データストレージ拡張性：Blob Storage は無制限のストレージ拡張をサポート、将来的に動画、画像などのリソースを追加する際、直接ストレージを拡張でき、ストレージ不足の問題を心配する必要はない；Azure SQL Database は異なる地域への拡張をサポート、将来的にユーザーが広範囲に分布する場合、読み取り専用レプリカを追加し、クエリパフォーマンスを向上。

#### 8.2.3 技術拡張性（チームの技術向上と技術イテレーションに適応）

1.  アーキテクチャの拡張ポイントの予約：フロントエンドのミニマム MVVM アーキテクチャは後続で段階的に完全な MVVM+ライトウェイト Clean Architecture にアップグレード可能、バックエンドのライトウェイト Clean Architecture は後続でマイクロサービスに分割可能、アーキテクチャの拡張ポイントを予約；
2.  技術スタックの互換性：フロントエンドは RN+TS、React+TS を採用、バックエンドは Spring Boot を採用、いずれも業界の主流技術スタックであり、コミュニティエコシステムが充実しており、技術イテレーションとバージョンアップが容易；Azure SDK は複数の言語とフレームワークをサポート、後続の技術スタックの拡張が容易；
3.  コード拡張性：標準化、汎用化設計を採用、コードの再利用性が高く、後続の技術イテレーション、機能拡張時に重複開発を減少、開発効率を向上。

## 9. 可用性とディザスタリカバリ

本システムは Azure クラウドサービスのデュアルライブデプロイ、異なる地域でのレプリケーション、バックアップとリストア能力に基づき、高可用性のシステムアーキテクチャを構築、完善なディザスタリカバリ戦略を策定、システムの可用性を保障、災害による損失を低減、保険業務のサービス継続性のニーズに適応する。

### 9.1 可用性設計（目標：システム可用性 ≥99.9%）

#### 9.1.1 App Service のプライマリ・セカンダリデュアルライブデプロイ

1.  デプロイアーキテクチャ：App Service は Primary（プライマリインスタンス）、Secondary（セカンダリインスタンス）のデュアルライブデプロイを採用、2 つのインスタンスは異なる可用性ゾーンに配置、単一の可用性ゾーンの障害によるサービス中断を回避；
2.  トラフィック分配：通常時、すべてのトラフィックは Azure Front Door を介して Primary インスタンスに転送、Secondary インスタンスは待機状態で、Primary インスタンスのコードと設定を同期；
3.  障害切り替え：Azure Front Door は 30 秒間隔で 2 つのインスタンスのヘルスチェックを定期的に実行、Primary インスタンスに障害（サービス利用不可、インターフェース呼び出し失敗）が発生し、連続して 3 回のヘルスチェックに失敗した場合、自動的にすべてのトラフィックを Secondary インスタンスに切り替え、切り替え時間 ≤1 分間、サービスの継続性を保障；
4.  インスタンス同期：Azure DevOps CD パイプラインにより、Primary と Secondary インスタンスのコード、設定、バージョンが完全に一致することを確保、障害切り替え後のサービスに差異がないようにする。

#### 9.1.2 Azure SQL Database のプライマリ・セカンダリデュアルライブデプロイ

1.  デプロイアーキテクチャ：Azure SQL Database は Primary（プライマリデータベース）、Secondary（セカンダリデータベース）のデュアルライブデプロイを採用、異なる地域でのレプリケーション機能を有効にし、2 つのデータベースは異なる可用性ゾーンに配置、データはリアルタイムで同期（同期遅延 ≤1 秒）；
2.  障害転送：自動障害転送機能を有効にし、Primary プライマリデータベースに障害（データベースクラッシュ、ネットワーク中断）が発生した場合、Azure は自動的に Secondary セカンダリデータベースを Primary プライマリデータベースに昇格、障害転送時間 ≤1 分間、データの継続性と可用性を保障；
3.  読み書き分離：通常時、家族紐付け、コンテンツ作成、スタンプ記録更新などのすべての書き込み操作は Primary プライマリデータベースを指し、契約閲覧、コンテンツクエリなどの読み取り操作はニーズに応じて Secondary セカンダリデータベースを指すことができ、読み書き分離を実現、データベースのパフォーマンスと可用性を向上；
4.  データ整合性：Azure SQL の異なる地域でのレプリケーション機能により、プライマリ・セカンダリデータベースのデータがリアルタイムで同期することを確保、障害転送後にデータ損失がなく、データ整合性を保障。

#### 9.1.3 その他のサービスの可用性保障

1.  Azure Front Door：複数の可用性ゾーンでのデプロイを採用、自身が高可用性を備え、ゲートウェイの単一障害点を回避；
2.  Blob Storage：異なる地域での冗長ストレージを有効にし、データを複数の地域でバックアップ、単一の地域に障害が発生しても、他の地域からリソースをリストアできる；
3.  Redis Cache：マスタースレーブアーキテクチャを採用、マスターノードに障害が発生した後、自動的にスレーブノードに切り替え、キャッシュサービスの可用性を保障；
4.  Notification Hubs：複数の可用性ゾーンでのデプロイを採用、Push プッシュサービスの継続的な可用性を保障、プッシュの失敗を回避。

### 9.2 ディザスタリカバリ設計

#### 9.2.1 災害レベル分類（Azure クラウドサービスの障害シナリオに基づく）

1.  レベル 1 災害：単一のサービスインスタンスの障害（Primary App Service インスタンスの障害、Primary SQL インスタンスの障害など）、一部のサービスまたは一部のユーザーに影響；
2.  レベル 2 災害：単一の可用性ゾーンの障害（Primary インスタンスの所在する可用性ゾーンの障害など）、プライマリインスタンスのサービスに影響；
3.  レベル 3 災害：地域レベルの障害（プライマリ地域の障害など）、すべてのプライマリ・セカンダリインスタンスのサービスに影響；
4.  レベル 4 災害：データ損失/改ざん（データベースデータの誤削除、悪意のある改ざんなど）、業務の正常運行に影響。

#### 9.2.2 ディザスタリカバリ戦略（異なる災害レベルに対応）

##### 9.2.2.1 レベル 1 災害リカバリ（単一のサービスインスタンスの障害）

- リカバリトリガー：Azure Front Door のヘルスチェックにより Primary App Service インスタンスの障害が発見された場合、または Azure SQL により Primary プライマリデータベースの障害が自動的に検出された場合；
- リカバリプロセス：
  1.  App Service 障害：Front Door は自動的にトラフィックを Secondary インスタンスに切り替え、運用管理者は Primary インスタンスの障害（サービスクラッシュ、設定異常など）を調査し、修復後にトラフィックを Primary インスタンスに切り戻す；
  2.  SQL 障害：Azure は自動的に Secondary セカンダリデータベースを Primary プライマリデータベースに昇格、運用管理者は元の Primary プライマリデータベースの障害を調査し、修復後に再度 Secondary セカンダリデータベースとして構成、データを同期；
- リカバリ目標：RTO（復旧時間目標）≤5 分間、RPO（復旧ポイント目標）≤1 分間、データ損失なし、ユーザーに明らかな感知はない。

##### 9.2.2.2 レベル 2 災害リカバリ（単一の可用性ゾーンの障害）

- リカバリトリガー：Primary インスタンスの所在する可用性ゾーンに障害が発生し、Primary App Service、Primary SQL インスタンスが利用不可となった場合；
- リカバリプロセス：
  1.  Front Door は自動的にすべてのトラフィックを他の可用性ゾーンに所在する Secondary インスタンスに切り替え、サービスの正常運行を保障；
  2.  Azure SQL は自動的に Secondary セカンダリデータベースを Primary プライマリデータベースに昇格、データサービスの提供を継続；
  3.  運用管理者は可用性ゾーンの障害状態を監視、可用性ゾーンの復旧後、Primary インスタンス、Primary SQL プライマリデータベースを再デプロイ、データと設定を同期、デュアルライブデプロイを復旧；
- リカバリ目標：RTO≤10 分間、RPO≤1 分間、データ損失なし、ユーザーに明らかな感知はない。

##### 9.2.2.3 レベル 3 災害リカバリ（地域レベルの障害）

- リカバリトリガー：プライマリ地域に障害が発生し、すべてのプライマリ・セカンダリインスタンス、データベース、クラウドサービスが利用不可となった場合；
- リカバリプロセス：
  1.  異なる地域でのバックアップを有効にし、予備地域に臨時の App Service インスタンス、Azure SQL データベースをデプロイ、バックアップデータをリストア（Azure Backup からリストア）；
  2.  DNS 解決を修正、トラフィックを予備地域の臨時サービスに切り替え、サービスの可用性を保障；
  3.  プライマリ地域の復旧後、臨時サービスのデータをプライマリ地域に同期、デュアルライブアーキテクチャを再デプロイ、トラフィックをプライマリ地域に切り戻す；
- リカバリ目標：RTO≤1 時間、RPO≤1 時間、データ損失を最小限に抑え、コア業務の可用性を保障。

##### 9.2.2.4 レベル 4 災害リカバリ（データ損失/改ざん）

- リカバリトリガー：データベースデータの誤削除、悪意のある改ざん、または Blob Storage リソースの損失が発生した場合；
- リカバリプロセス：
  1.  関連するサービスを停止（データの更なる損傷を回避）、Azure Backup から指定した時点のデータ（30 日間以内の任意の時点）をリストア、臨時データベースにリストア；
  2.  リストアしたデータの完全性と整合性を検証、データに誤りがないことを確認後、運用環境のデータを置き換え；
  3.  関連するサービスを復旧、スモークテスト、インターフェーステストを実行、サービスの正常運行を確認；
  4.  データ損失/改ざんの原因を調査、セキュリティ防御を強化、再発を回避；
- リカバリ目標：RTO≤30 分間、RPO≤1 時間、損失/改ざんされたデータを復旧、業務の正常運行を保障。

### 9.3 バックアップとリストア戦略（汎用テンプレート）

#### 9.3.1 バックアップポリシー

| データ種別               | バックアップ方式                                    | バックアップ頻度               | 保有期間 | バックアップストレージ位置                  |
| ------------------------ | --------------------------------------------------- | ------------------------------ | -------- | ------------------------------------------- |
| Azure SQL Database       | Azure Backup による自動バックアップ                 | 毎日フル＋ 4 時間ごと増分      | 30 日間  | Azure Backup 専用ストレージ                 |
| Blob Storage（リソース） | Azure Blob スナップショット＋異地域レプリケーション | 毎日スナップショット           | 7 日間   | 異地域の Blob Storage                       |
| App Service 設定/コード  | Azure DevOps コードリポジトリ＋設定バックアップ     | デプロイごとに自動バックアップ | 90 日間  | Azure Repos+Azure Artifacts                 |
| ログデータ               | Log Analytics による自動アーカイブ                  | 毎日アーカイブ                 | 180 日間 | 異地域の Blob Storage（コールドストレージ） |

#### 9.3.2 リストアテスト

- テスト頻度：毎月リストアテストを実行、毎四半期にディザスタリカバリ演習（レベル 1、レベル 2 の災害シナリオを模擬）を実行；
- テスト内容：バックアップデータの完全性、リストアプロセスの実行可能性、リストア時間が RTO/RPO 目標を満たすかどうかを検証；
- テストプロセス：Azure Backup からテスト環境にデータをリストア、データ整合性を検証、機能テストを実行、リストア時間とテスト結果を記録、リストアプロセスを最適化。

### 9.4 可用性監視と保障

1.  リアルタイム監視：Application Insights、Azure Monitor により App Service、SQL、Front Door などのコアサービスの運行状態をリアルタイムに監視、サービス利用不可アラートを構成、可用性の問題を速やかに発見；
2.  定期的な点検：運用管理者は毎日システムの可用性を点検、監視ダッシュボード、ログを閲覧、潜在的な問題を調査；毎週バックアップの状態を確認、バックアップの正常実行を確認；
3.  障害対応計画：異なる災害レベルに対する詳細な障害処理計画を策定、責任者、処理ステップ、リストアプロセスを明確にし、障害発生時に迅速に対応できるようにする；
4.  冗長性保障：すべてのコアサービスにはデュアルライブデプロイ、異なる地域でのバックアップを採用、単一障害点を回避；機微データは複数のコピーでストレージ、データの安全性と可用性を保障。
