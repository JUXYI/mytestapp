这是一份为您量身定制的 **PPT 提案大纲**。这份大纲结构符合日本企业（及国际咨询公司）的汇报习惯：**结论先行、逻辑严密、细节详实**。

您可以直接按照这个结构制作 PPT，每一页的**核心要点（Key Message）**和**建议配图**我都为您列出来了。

---

# 📑 新版保险 App 家庭连携与合同管理架构提案 (PPT Structure)

## 第一部分：背景与核心构想 (Executive Summary)

### Slide 1: 封面 (Title Slide)

- **标题：** 新版 App 家庭连携与合同管理功能 架构设计提案
- **副标题：** 基于 React Native + Azure 的 DX 体验升级方案
- **日期：** 202X 年 X 月 X 日
- **汇报人：** 产品经理 (PM)

### Slide 2: 核心摘要 (Executive Summary)

- **现状痛点：** 现有 WebView 体验割裂，家庭连携率低，高龄用户触达难。
- **本次目标：**

1. **体验升级：** 实现“扫码绑定”与“无缝查阅”。
2. **架构解耦：** 引入 Azure BFF 层，降低老系统负担。
3. **安全合规：** 确保 PII（个人敏感信息）的最小化授权与脱敏。

- **结论先行：** 建议采用 **“分阶段实施策略”** ——
- **Phase 1:** 上线**方案二（App 原生查阅）**，确保核心体验。
- **Phase 2:** 迭代**方案三（代理授权）**，覆盖高龄人群。

---

## 第二部分：三种方案详解 (Detailed Solutions)

### Slide 3: 方案概览：三种模式的定位 (Overview)

- _用一张光谱图展示三个方案的权衡_
- **方案一 (Web Linkage)：** **稳健型**。利用现有 Web 资产，开发成本最低，但体验一般。
- **方案二 (Native Integration)：** **体验型**。App 内原生闭环，体验最佳，也是行业主流方向。
- **方案三 (Proxy Auth)：** **增长型**。在方案二基础上，增加“免 App 绑定”，消除数字鸿沟。

---

### Slide 4: 【方案一】Web 跳转浏览模式 (Web Linkage Model)

- **核心逻辑：** App 负责绑定，查阅时通过 SSO 跳转至 MyPage 网页。
- **架构亮点 (修正版)：**
- **Azure 双写：** 绑定关系同时写入 Azure (用于列表加速) 和 Legacy (用于 Web 鉴权)。
- **Payload 注入：** 针对 Legacy 无法扩表的问题，采用 Ticket 携带权限参数的无状态模式。

- **配图建议：** (插入之前生成的 Mermaid 图)

### Slide 5: 【方案一】优劣势与适用场景

- **优势：** Legacy 系统改造极小（只需接收 Ticket Payload），上线速度快。
- **劣势：** 页面跳转导致用户流失；App 无法对数据进行二次利用（如无法做家庭保障分析）。
- **适用场景：** 预算极度有限，或 Legacy 页面逻辑过于复杂短期无法重写。

---

### Slide 6: 【方案二】App 原生查阅模式 (Native Integration Model) ★ 推荐

- **核心逻辑：** App 原生展示，Azure 充当数据中枢 (BFF)。
- **架构亮点 (最新版)：**
- **Azure 主导鉴权：** “谁能看谁”完全由 Azure DB 判断。
- **数据脱敏 (Masking)：** Azure 使用服务账号获取全量数据，根据权限清洗敏感字段（如隐藏金额）后返回给 App。

- **配图建议：** (插入之前生成的 Mermaid 图)

### Slide 7: 【方案二】优劣势与适用场景

- **优势：** 极致流畅的 UX；数据结构化，为未来的 AI 投保建议打基础；Legacy 只需提供“裸数据”，责任边界清晰。
- **劣势：** App 端和 Azure 端开发工作量较大。
- **适用场景：** 追求品牌形象，希望提升 App 月活 (MAU) 的项目。

---

### Slide 8: 【方案三】代理授权模式 (Proxy Auth Model)

- **核心逻辑：** 解决“父母不下载 App”的问题。
- **流程演示：**

1. 子女输入信息 -> 2. 父母收短信 -> 3. H5 页面点“同意” -> 4. 绑定成功。

- **技术关键：** 短信网关集成 + 轻量级 H5 安全落地页。
- **配图建议：**

---

## 第三部分：深入对比与决策支持 (Analysis & Decision)

### Slide 9: 全维度对比分析 (Comparison Matrix)

- _插入之前整理的对比表格_
- **重点强调：**
- **用户体验：** 方案二/三 完胜 方案一。
- **家庭覆盖率：** 方案三 独占鳌头（父母无需 App）。
- **安全性：** 三者均符合金融级安全标准（方案一靠 Ticket，方案二靠 Azure Masking）。

### Slide 10: 系统修改点影响分析 (System Impact)

- _插入之前整理的“系统修改点清单”表格_
- **关键信息：**
- **MyPage (Legacy) 团队：**
- 方案一需改动 SSO 逻辑（Payload 注入）。
- 方案二需提供原始数据接口（Admin API）。

- **MyApp (Azure) 团队：**
- 方案二的工作量主要在 BFF 层的“数据聚合与脱敏”。

---

## 第四部分：实施建议 (Roadmap)

### Slide 11: 推荐实施路线图 (Phased Roadmap)

- **Phase 1: MVP 上线 (方案二)**
- 目标：完成 React Native 重构，上线原生家庭查阅功能。
- 对象：核心用户及其配偶/子女（智能手机熟练用户）。
- 理由：树立 DX 标杆，摒弃过时的 WebView。

- **Phase 2: 增长迭代 (方案三)**
- 目标：新增“免 App 代理绑定”功能。
- 对象：60 岁以上高龄父母。
- 理由：在 App 稳定后，通过此功能拉动“家庭连接数” KPI。

### Slide 12: 下一步行动计划 (Next Steps)

1. **接口评审：** 确认 MyPage 团队能否提供 `GET /raw-data/contracts` (方案二) 或 `create-ticket` 改动 (方案一)。
2. **合规确认：** 确认“代理绑定”的短信文案及法务条款。
3. **环境搭建：** 启动 Azure SQL `FamilyRelations` 表结构设计。

---

### Slide 13: 附录 (Appendix)

- 详细的 API 定义草稿
- Azure DB ER 图设计草案
- 权限控制矩阵 (Permission Matrix)

---

### 💡 演讲时的 Tips (给 PM 的小抄)

1. **讲到 Slide 4 (方案一) 时：** 强调我们想出了“Payload 注入”这个天才的想法，解决了老系统无法加表的难题，体现了我们的技术解决能力。
2. **讲到 Slide 6 (方案二) 时：** 强调“Azure 鉴权”不仅是为了现在，更是为了未来。一旦逻辑收归 Azure，以后做微信小程序、LINE 小程序都能复用这套逻辑，不用再改老系统。
3. **讲到 Slide 11 (路线图) 时：** 强调这是“进可攻退可守”的策略。Phase 1 稳扎稳打，Phase 2 带来惊喜。

もちろんです。これが日本のお客様（ステークホルダー）向けの**最終提案書（日本語版）**です。

日本のビジネス習慣に合わせ、論理構成（Logic Flow）を整え、「現状の課題」から「解決策」、「具体的な実装イメージ」、そして「ロードマップ」へと繋がるストーリーラインにしています。

このまま PPT の構成案として、あるいはドキュメントとしてご使用いただけます。

---

# 提案書：新保険アプリにおける「家族連携・契約管理」機能 アーキテクチャ提案

**作成日：** 202X 年 X 月 X 日
**作成者：** プロダクトマネージャー (PM)
**対象：** プロジェクトオーナー、システム企画部、開発チーム

---

## 1. はじめに (Introduction)

### 背景と目的

現在、Yappli 版アプリからのリプレイスに伴い、**React Native + Azure** を活用したモダンなアーキテクチャへの刷新を計画しています。
特に、顧客ロイヤリティ向上と契約継続率の改善に直結する**「家族連携（Family Binding）」**および**「家族の契約照会」**機能について、最適な実装方式をご提案します。

### 本提案のゴール

1. **UX の刷新：** 既存の WebView 依存を脱却し、ネイティブアプリならではの快適な操作性を実現する。
2. **開発効率と品質のバランス：** 既存資産（MyPage 基幹）への影響を最小限に抑えつつ、Azure 側で柔軟な機能拡張を可能にする。
3. **セキュリティとコンプライアンス：** 個人情報保護（PII）の観点から、家族間の閲覧権限を厳格に管理する。

---

## 2. 提案スキーム概要 (Solution Overview)

実現難易度とユーザー体験（UX）のバランスが異なる 3 つの案を検討しました。

- **案 ①：Web 遷移モデル (Web Linkage)**
- アプリは「入り口」に徹し、詳細は既存の MyPage（Web）へ SSO 遷移して表示する**堅実なプラン**。

- **案 ②：ネイティブ完結モデル (Native Integration) ★ 推奨**
- Azure をデータハブとし、アプリ内で情報の閲覧を完結させる**UX 重視のプラン**。

- **案 ③：代理登録モデル (Proxy Auth)**
- 案 ② に加え、アプリを操作できない高齢者向けに「SMS ＋ Web」での承認フローを追加した**利用者拡大プラン**。

---

## 3. 各スキームの詳細仕様 (Detailed Specs)

### 【案 ①】Web 遷移モデル (Web Linkage Model)

**コンセプト：** 既存 Web 資産の最大活用と、Azure によるリスト表示の高速化。

- **業務フロー：**
- **連携時：** Azure 上の DB に家族関係を保存し、アプリのリスト表示を高速化。基幹システムへの連携は行わず（または不可とし）、Azure を正（マスター）とする。
- **閲覧時：** アプリから SSO チケット発行を要求する際、**「閲覧権限情報（Payload）」をパラメータとして渡す**。

- **技術的ポイント：ペイロード注入 (Payload Injection)**
- 基幹側に「家族権限テーブル」を新設できない制約を回避するため、SSO チケット発行時に権限情報（例：`{show_amount: true}`）を同梱します。
- 基幹側は、Web セッション生成時にこの情報を読み込み、画面の出し分け（金額マスク等）を行います。

**▼ システム変更点 (Impact Analysis)**
| システム | 変更箇所 | 内容 |
| :--- | :--- | :--- |
| **MyApp**

(Azure/RN) | **DB 新設** | `FamilyRelations` テーブル作成（関係・権限の正データ）。 |
| | **API** | 閲覧前に Azure から最新権限を取得し、基幹 SSO API へ渡すロジック実装。 |
| **MyPage**

(Legacy) | **SSO API** | チケット生成時に任意の Payload（JSON 等）を受け取り、一時保存する改修。 |
| | **Web 画面** | セッション内の Payload 情報を参照し、項目の表示/非表示を制御する。 |

---

### 【案 ②】ネイティブ完結モデル (Native Integration Model)

**コンセプト：** Azure BFF によるデータ集約とセキュリティ制御。

- **業務フロー：**
- **連携時：** Azure DB に家族関係を保存。基幹システムへは、コールセンター対応等のために**非同期（バックグラウンド）で同期**を行う。
- **閲覧時：** アプリは Azure にデータを要求。**Azure が権限チェックとデータ加工（マスキング）**を行った上で、アプリに JSON を返す。

- **技術的ポイント：Azure による中央集権管理**
- 「誰が誰を見られるか」のロジックは全て Azure が持ちます。
- 基幹システムは、Azure からの特権アクセスに対し、全量データを返すだけの「データプロバイダ」に徹します。

**▼ システム変更点 (Impact Analysis)**
| システム | 変更箇所 | 内容 |
| :--- | :--- | :--- |
| **MyApp**

(Azure/RN) | **BFF 実装** | 基幹から生データを取得し、権限に基づいて項目を削除・マスクするロジックの実装。 |
| | **UI 開発** | 契約一覧・詳細画面のネイティブ UI コンポーネント開発（工数大）。 |
| **MyPage**

(Legacy) | **API** | **特権 API の提供**：`GET /admin/raw-data`。Azure からの要求に対し、指定ユーザーの全データを返す（ビジネスロジック不要）。 |

---

### 【案 ③】代理登録モデル (Proxy Auth Model)

**コンセプト：** デジタルデバイド（高齢者のスマホ利用の壁）の解消。

- **業務フロー：**
- **連携時：** 子（アプリ利用者）が親の情報を入力。Azure が SMS を送信。
- **承認時：** 親は SMS のリンクから**ログイン不要の Web 画面**を開き、「許可」ボタンを押すだけで連携完了。

- **技術的ポイント：外部認証基盤**
- Azure 上で SMS ゲートウェイ連携と、軽量な承認用 Web ページをホスティングします。

**▼ システム変更点 (Impact Analysis)**
| システム | 変更箇所 | 内容 |
| :--- | :--- | :--- |
| **MyApp**

(Azure) | **機能追加** | SMS 送信機能、および承認用 Web ページ（H5）の開発。 |
| **MyPage**

(Legacy) | **API** | **実在確認 API**：入力された「氏名＋電話番号」が顧客 DB に存在するかチェックする API の提供。 |

---

## 4. 比較と意思決定 (Comparison & Recommendation)

| 比較項目              | **案 ① Web 遷移**  | **案 ② ネイティブ完結** | **案 ③ 代理登録** |
| --------------------- | ------------------ | ----------------------- | ----------------- |
| **ユーザー体験 (UX)** | △ **Web 遷移**<br> |

<br>画面遷移が発生し、操作感が分断される | ◎ **シームレス**<br>

<br>アプリ内で完結し、高速・快適 | ◎ **シームレス**<br>

<br>アプリ内完結 ＋ 登録のハードル低 |
| **家族連携率** | △ 低い<br>

<br>家族全員のアプリ DL が必須 | △ 低い<br>

<br>家族全員のアプリ DL が必須 | ★ **高い**<br>

<br>親世代は**アプリ不要**で連携可能 |
| **開発コスト** | ◎ **低い**<br>

<br>既存 Web 画面を流用 | △ **中～高**<br>

<br>UI とデータ変換ロジックの開発が必要 | ▲ **高い**<br>

<br>SMS 連携等の追加開発が必要 |
| **基幹への影響** | ▲ **あり**<br>

<br>SSO/Session ロジックの改修が必要 | ◎ **極小**<br>

<br>単純なデータ提供 API のみで OK | ◎ **極小**<br>

<br>案 ② と同様 |
| **データ活用** | × 困難<br>

<br>画面表示のみでデータを持たない | ◎ **可能**<br>

<br>構造化データとして蓄積、分析可能 | ◎ **可能**<br>

<br>同上 |

---

## 5. 推奨ロードマップ (Implementation Roadmap)

プロジェクトのリスク低減と早期の価値提供のため、**段階的なリリース**を提案します。

### Phase 1：MVP リリース（案 ② ネイティブ完結モデル）

- **ターゲット：** スマホリテラシーのある現役世代（夫婦、子）。
- **採用理由：**
- アプリのリニューアルにおいて、WebView への遷移は UX 観点で許容し難い。
- 基幹システム側の改修ロジック（特権データ提供のみ）がシンプルであり、プロジェクト遅延リスクを Azure 側でコントロールしやすいため。
- **注：** 案 ① の SSO ロジックは、将来的な Web 専用機能（約款確認など）のために、共通基盤として並行開発することを推奨します。

### Phase 2：グロースハック（案 ③ 代理登録モデル）

- **ターゲット：** アプリ操作が苦手な高齢の親世代。
- **採用理由：**
- アプリの安定稼働後、KPI（家族接続数）を最大化するための切り札として投入。
- 「親の保険管理」は日本市場において極めてニーズの高いユースケースであるため。

---

## 6. 次のアクション (Next Steps)

本提案のご承認後、以下のステップへ進めさせていただきます。

1. **インターフェース定義（API 仕様策定）**

- 基幹チーム様へ：`GET /admin/raw-data`（生データ取得）および `POST /check-user`（実在確認）の仕様調整。

2. **Azure DB 設計**

- `FamilyRelations` テーブルの詳細設計（権限 JSON のスキーマ定義など）。

3. **法務確認（案 ③ 向け）**

- SMS による代理承認フローの利用規約および免責事項の確認。

---

以上、ご提案申し上げます。
貴社の DX 推進と顧客エンゲージメント向上に、本アーキテクチャが貢献できると確信しております。
