# 新版保险 App 家庭连携与合同管理方案说明书

## 1. 方案概览

针对“家庭成员合同管理”这一核心需求，为了平衡**用户体验 (UX)**、**开发成本**以及**日本市场的特殊性**，我们设计了以下三种方案：

- **方案一 (Web 跳转模式)：** 务实之选。App 仅作为入口，核心数据展示依赖现有的 MyPage 网页。
- **方案二 (App 原生模式)：** 体验之选（推荐）。利用 Azure 云服务聚合数据，在 App 内实现流畅的原生查阅体验。
- **方案三 (代理授权模式)：** 增长之选（进阶）。针对不擅长使用智能手机的长辈，提供短信+网页的简易绑定方式。

---

## 2. 方案详细说明

### 方案一：Web 跳转浏览模式 (Web Linkage Model)

**核心理念：**
利用 App 便捷的绑定功能建立关系，但在查阅合同时，安全跳转至现有的 MyPage 网页。这是一种“低成本、快上线”的过渡方案。

**【流程 A】家庭绑定流程 (Azure 托管)**

1. **邀请：** 用户 A 在 App 点击“邀请家人”，App 请求 Azure 后端生成一个**邀请码/二维码**。
2. **输入：** 用户 B（家人）下载 App 并登录，输入该邀请码。
3. **验证与同步（关键点）：**

- Azure 验证验证码有效。
- **Azure 立即向 MyPage 老系统发起同步请求**，告知“A 和 B 建立了关系”。
- _注意：必须确保老系统数据库写入成功，否则后续跳转网页时会报错。_

4. **完成：** App 提示绑定成功。

**【流程 B】合同查阅流程 (SSO 跳转)**

1. **点击：** 用户 A 点击“查看用户 B 的合同”。
2. **请求票据：** App 直接向 **MyPage 老系统**请求一个“一次性入场券 (Ticket)”。
3. **跳转：** App 拿到 Ticket，自动唤起手机浏览器（Safari/Chrome）。
4. **验证与展示：**

- 浏览器访问 MyPage 网页。
- MyPage 验证 Ticket 合法并立即销毁（防止被盗用）。
- MyPage 建立登录态，直接展示用户 B 的合同详情页。

---

### 方案二：App 原生查阅模式 (Native Integration Model)

**核心理念：**
所有操作都在 App 内闭环完成。通过 Azure 构建“数据中台 (BFF)”，清洗老系统的数据并以原生卡片形式展示，提供最佳体验。

**【流程 A】家庭绑定流程 (Azure 托管)**

1. **邀请与输入：** 与方案一完全相同（生成邀请码 -> 输入验证）。
2. **验证与存储（关键点）：**

- Azure 验证通过后，将家庭关系主要存储在 **Azure 数据库**中。
- Azure 会在后台**异步（悄悄地）**将关系备份给老系统，保持数据一致性，但不会阻塞用户操作。

3. **完成：** 用户立刻看到绑定成功，响应速度比方案一更快。

**【流程 B】合同查阅流程 (原生展示)**

1. **点击：** 用户 A 点击“查看用户 B 的合同”。
2. **获取数据：** App 向 **Azure 后端**发起请求。
3. **权限与脱敏：**

- Azure 检查 A 是否有权看 B。
- Azure 从老系统调取原始数据。
- **Azure 进行数据“清洗”：** 例如，根据权限隐藏“受益人”或“解约返利金”等敏感字段。

4. **展示：** App 接收到处理好的数据，在界面上渲染出原生卡片。用户切换家人时无缝丝滑，无需等待网页加载。

---

### 方案三：代理授权模式 (Proxy Auth Model)

**核心理念：**
这是方案二的**增强插件**。解决“父母年纪大，不会下载 App，导致子女无法在 App 里管理全家保单”的痛点。

**【流程 A】简易绑定流程 (短信 + H5)**

1. **申请：** 子女（用户 A）在 App 输入父母（用户 B）的姓名、电话和生日。
2. **校验：** 系统核对父母在保险公司的预留信息是否一致。
3. **短信触达：** 系统向父母的手机发送一条短信，内含一个安全链接。
4. **网页确认：**

- 父母点击链接，打开一个**极其简单的网页**。
- 网页显示：“您的孩子 A 申请查看您的保单，是否同意？”
- 父母点击“同意”按钮即可。（**无需下载 App，无需注册账号**）。

5. **完成：** 子女的 App 收到推送通知，绑定成功。

**【流程 B】合同查阅流程**

- 与 **方案二** 完全一致（在 App 内原生查看）。

---

## 3. 方案对比总结

| 特性         | 方案一 (Web 跳转) | 方案二 (App 原生) | 方案三 (代理授权) |
| ------------ | ----------------- | ----------------- | ----------------- |
| **查阅体验** | **一般**<br>      |

<br>需跳转浏览器，有割裂感 | **极佳**<br>

<br>App 内秒开，交互流畅 | **极佳**<br>

<br>App 内秒开 |
| **绑定门槛** | **高**<br>

<br>全家必须下载 App | **高**<br>

<br>全家必须下载 App | **低**<br>

<br>父母**无需下载 App** |
| **开发重心** | MyPage 老系统的 SSO 接口改造 | Azure 端的数据聚合与脱敏逻辑 | 短信网关与外部 H5 页面开发 |
| **核心优势** | 利用现有网页资产，开发最快 | 现代化体验，提升 App 粘性 | 消除数字鸿沟，最大化家庭连接数 |

## 4. 实施建议

为了兼顾**上线速度**与**长期价值**，建议采取分阶段实施：

1. **第一阶段 (MVP)：采用方案二。**
   既然决定重构 App，就应彻底抛弃“网页套壳”的旧体验。优先保证拥有智能手机的核心用户群（30-50 岁）能获得最佳的原生查阅体验。
2. **第二阶段 (Growth)：升级至方案三。**
   在 App 运行稳定后，追加“免 App 代理授权”功能，以此为抓手，将 60 岁以上的高龄客户群体纳入数字化服务体系。
