# 家庭连携功能改造｜可直接复制使用的提示词（mypage + myapp）

> 用法：把下面「✅ 提示词」整段复制给模型；把其中的 `<>` 占位符替换成你项目的实际情况。

---

## ✅ 提示词

你是资深互联网保险业务的「产品经理 + 解决方案架构师 + 客户端/服务端技术负责人」。
我会给你一段现状、目标、约束与候选方案。你需要先提出必须澄清的问题（不超过 12 个，按优先级 P0/P1/P2 排序）。
在我补充信息之前，你仍要基于当前信息给出“可落地”的初版方案，并把关键假设写清楚。

### 1) 背景（已知）

现有产品包含：

- **mypage（网页端）**：支持保险合同查看、家庭成员添加；暂不支持查看“已添加家庭成员”的保险合同信息。
- **myapp（移动端）**：登录界面通过 WebView 调用 mypage 网页实现；登录后可点击按钮跳转至浏览器打开 mypage；当前无家庭相关联查功能。

### 2) 改造目标

新增“家庭连携（跨账号绑定亲属关系）”能力：

- 支持跨账号绑定（发起方/被绑定方）。
- 双方在**授权**后可互相查看对方保险合同信息（授权可改、可解除）。
- 需适配既有登录与页面跳转逻辑（WebView 登录 + 浏览器打开 mypage）。
- 本次改造范围包含 **SSO/免登链路** 的设计与落地（想定使用 ticket 或 token 方案）。

### 3) 两种候选方案（请你比较并给出推荐）

#### 方案一：仅 myapp 做绑定；mypage 实现全量查看

- 绑定流程：myapp 开发。
- 合同查看：mypage 开发。
- 跳转：myapp 需携带“绑定信息 + 权限信息”跳转并免登 mypage。
- 约束：
  - myapp **不访问/不调用** mypage 的 DB 及任何 API，因此无法读取 mypage 端已有家庭成员信息。
  - 即使在 myapp 完成成员绑定，仍需在 mypage 端**手动添加家庭成员**，才能查看该成员合同。
  - 合同信息展示全部依托 mypage；myapp 只做绑定与带参跳转。

#### 方案二：myapp 实现绑定 + 查看，移动端闭环

- 绑定流程、合同查看都在 myapp 开发。
- mypage 可保持原样（不做连查）。
- 约束：
  - myapp 需要调用 mypage 的 API 获取自身及已绑定成员合同数据，在 app 内展示。
  - 即使在 myapp 完成成员绑定，仍需在 mypage 端**手动添加家庭成员**，才能查看该成员合同。

### 3.1) 已确认的业务/技术约束（请你必须纳入设计）

- **账号体系**：mypage 与 myapp 为同一账号体系；SSO 也是本次改造对象。
- **免登**：想定通过 `ticket` 或 `token` 实现免登/单点；需要你给出推荐实现与风险控制。
- **合同查询**：mypage 现有“合同查询 API”仅能查询本人合同；如需查询家庭合同，可新增 API。
- **连携码/URL**：连携码有效期 24 小时、一次性；不可撤回但可重新生成（新码生成后旧码立即失效）；URL **不允许携带敏感信息**。
- **深链能力**：iOS Universal Links / Android App Links 已接入，可直接使用。
- **关系与授权**：仅当关系类型为“亲人”时，才允许授权对方查看合同；“其他亲属/其他”仅允许绑定，不提供合同互查授权能力。

### 4) 统一“家庭连携”绑定流程（两方案通用）

以“发起绑定方”在 myapp 的操作为主：

1. 发起绑定：支持跨账号绑定；绑定前选择关系类型（家人/其他亲属/其他）。
2. 生成连携码：系统生成唯一连携码，可展示为二维码/可复制 URL。
3. 被绑定方未安装 myapp：扫码/点链接 → 应用商店 → 安装/注册 → 进入“连携码自动输入页面” → 确认 → 绑定完成。
4. 被绑定方已安装 myapp：扫码/点链接 → 直接打开 myapp 的“连携码自动输入页面” → 确认 → 绑定完成。
5. 权限设置：绑定确认后，双方可配置“自身合同信息是否授权给对方查看”（授权后对方可查看全部合同）。
6. 配套：双方均可发起解除；双方均可随时修改授权。

---

## 你需要输出的内容（必须按以下结构）

### A. 澄清问题（<= 12 个）

- 用 P0/P1/P2 分级，P0 表示“不回答会导致方案不可落地或风险极高”。

### B. 初版假设（信息不足时先写清假设）

- 列出你做方案设计时使用的关键假设（账号体系、鉴权方式、合同查询能力、合规要求等）。

### C. 方案对比与推荐（必须给明确结论）

用表格对比方案一/方案二：

- 用户体验闭环
- 技术复杂度
- 改造成本与周期
- 风险点（安全/合规/数据一致性）
- 对现有 mypage/myapp 架构侵入性

最后给出**明确推荐**（若推荐有前提条件，也要写清楚触发条件与备选方案）。

### D. 端到端业务流程（含异常分支）

- 分“发起方/被绑定方”视角描述。
- 必须覆盖：未安装/已安装、注册中断、连携码过期、重复绑定、关系类型选择、解除、授权修改。

并且你必须用 Mermaid 输出至少 3 张「业务流程图」来辅助说明（代码块必须可渲染）：

1. 绑定流程（含未安装/已安装、24 小时一次性、重新生成导致旧码失效）
2. 授权与合同查看判定流程（强调：仅“亲人”可授权；其他关系不可授权）
3. 解除与权限变更流程（双方发起、立即生效、边界条件）

图的要求：

- 优先使用 `flowchart TD`；节点文案用中文且简短。
- 必须显式标出关键状态（例如：待确认/已绑定/已解除/已过期）。
- 必须包含异常分支与回退动作（例如：token 过期 → 重新登录，连携码失效 → 提示重新生成）。

### E. 数据与权限设计（概念级即可）

- 建议的数据模型（实体：用户、连携关系、授权、连携码、状态流转）。
- 权限判定规则：何时能看谁的合同；解除后如何处理；授权变更是否即时生效。
- 必须体现“关系类型”对授权能力的约束（亲人可授权；其他不可）。

### F. 技术实现建议（按你推荐方案展开）

- 客户端：myapp / mypage 分别要新增哪些页面、入口、深链/Universal Link、参数、埋点。
- 服务端：需要哪些 API（列接口契约：路径、方法、入参、出参、错误码、鉴权方式）。
- 免登/跳转：token 传递策略、过期策略、重放防护、签名/nonce、URL 泄露风险控制。
- SSO 改造：WebView 登录态与系统浏览器登录态如何打通；myapp 与 mypage 间如何安全传递 ticket/token。

### G. 安全/合规/风控清单

- 明确“个人信息/合同信息”在链路中的潜在泄露点与对策。
- 日志脱敏、二维码/URL 有效期、一次性使用、不可在 URL 携带敏感信息的替代方案（例如短码换票据）、可选的二次确认/短信校验建议。

### H. 测试与验收（可执行）

- 给出关键用例（>= 15 条），含正向/逆向。
- 给出验收口径（例如：授权前不可见、解除后立即不可见、不同端一致性）。

### I. 里程碑与工作拆分

- 按 1~2 周迭代切分（列表即可），写清依赖与风险缓解。

---

## 可选补充信息（你可以向我索取）

若你认为信息不足，请优先向我索取以下内容（按必要程度提问）：

- <mypage 现有合同查询：是否已有 API？分页/筛选字段？>
- <家庭成员“添加”在 mypage 的数据结构与限制（数量/关系类型/实名要求）>
- <合规要求：是否允许查看“全部合同”？是否需要二次确认/短信验证？>
- <技术栈与约束：后端语言、网关、鉴权方式、是否有风控服务>

请开始输出。
